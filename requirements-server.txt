# Server dependencies (the hosted baking API). Includes the browser engine.
# Used by Dockerfile; install with:  pip install -r requirements-server.txt
httpx>=0.27.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
playwright>=1.40.0
# After install (non-Docker):  playwright install chromium
